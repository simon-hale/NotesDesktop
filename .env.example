# Notes Desktop 环境配置模板
#
# 用法：复制本文件为 .env.local（.gitignore 里 .env / .env.* 全部忽略，
# 只放行本模板），然后填入真实的后端地址。
# 所有环境相关地址只在这里维护，业务代码中禁止硬编码。
#
# 注意：VITE_* 变量会被打进客户端构建产物，因此这里只能放「地址」这类非机密配置，
# 绝对不要放密码、JWT、AccessKey、STS 凭证等任何 secret。

# Notes 后端根地址，不要带结尾斜杠。
#
# 改完这一个值就够了，它会被两处消费，注意两者的触发时机不同：
#   1) 前端 src/config.ts 读取为 VITE_API_BASE_URL —— 所有 REST 请求的 base，
#      由 npm run build / npm run dev（Vite）使用；
#   2) Rust src-tauri/build.rs 把它渲染进 src-tauri/capabilities/default.json
#      （模板见已提交的 default.json.example，生成物已 gitignore），
#      使 Tauri HTTP 权限只放行这一个后端域名。
#      —— build.rs 是 Rust 构建脚本，只有 cargo / tauri 命令会执行它：
#         cargo check / cargo build / npm run tauri:dev / npm run tauri:build。
#         npm run build 只做前端构建，不会生成该文件。
VITE_API_BASE_URL=https://your-notes-backend.example.com
